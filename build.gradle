// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
    alias(libs.plugins.android.library) apply false
}

subprojects { project ->
    afterEvaluate {
        if (project.plugins.hasPlugin("com.android.library")) {
            def androidExt = project.extensions.findByName("android")
            def moduleName = project.name
            def versionName = rootProject.ext.versionNameString

            androidExt.libraryVariants.all { variant ->
                variant.outputs.all { output ->
                    outputFileName = "${moduleName}-${versionName}.aar"
                }

                if (variant.name == 'release' || variant.name == 'debug') {
                    variant.assembleProvider.configure { assembleTask ->
                        assembleTask.doLast {
                            def aarDir = project.file("aar")
                            aarDir.mkdirs()
                            project.copy {
                                from project.file("build/outputs/aar/${moduleName}-${versionName}.aar")
                                into aarDir
                            }
                            println "Copied ${moduleName}-${versionName}.aar → ${aarDir}"
                        }
                    }
                }
            }
        }
    }
}

ext {
    androidXVersion = '1.3.1'
    versionCompiler = 36
    versionTarget = 33
    minSdkVersion = 21
    versionCode = 126
    versionNameString = '3.3.5'
    javaSourceCompatibility = JavaVersion.VERSION_11
    javaTargetCompatibility = JavaVersion.VERSION_11
//    ndkVersion = '21.0.6113669'
    supportLibVersion = '27.1.1'
    versionBuildTool = '27.0.3'
    kotlinCoreVersion = '2.0.21'
    kotlinCoroutines = '1.3.9'
    materialVersion = '1.3.0'
    constraintlayoutVersion = '2.0.4'
    lifecycle_version = '2.2.0'
    dialog_version= '3.2.1'
    bugly_version = '3.4.4'
    bugly_native_version = '3.9.0'
}
